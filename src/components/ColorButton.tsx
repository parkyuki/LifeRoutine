interface ColorButtonProps {
  colorName: string;
  Color: string;
  onClick: () => void;
  isSelect: boolean;
}

export const ColorButton = ({
  colorName,
  Color,
  onClick,
  isSelect,
}: ColorButtonProps) => {
  return (
    <div>
      <div></div>
    </div>
  );
};
